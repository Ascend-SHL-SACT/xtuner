# 代码开发总结报告

## 一、概述

**开发起点**: commit `ac7158f27cd11a7d6b29d73265a9fd5f6fb101a1` ("基本功能已经打通，MTP和SP均可使用")

**开发终点**: 当前HEAD

**主要工作**: 在Qwen3.5-MoE模型基础上进行Ascend NPU适配优化，包括GDN算子重写、Causal Conv1d优化、Cross Entropy Loss Triton实现、CPU/NPU绑核优化等功能。

---

## 二、功能模块总结

### 2.1 GatedDeltaNet (GDN) 算子优化

**修改文件**: `xtuner/v1/module/attention/gated_deltanet.py`

**优化内容**:

1. **RMS Norm优化**
   - 使用 `torch_npu.npu_rms_norm` 替代原有的PyTorch实现
   - 消除手动的float32转换和recompute逻辑
   - 减少不必要的dtype转换开销

2. **Delta Rule调用优化**
   - 从 `chunk_gated_delta_rule` 切换到 `flash_gated_delta_rule` (Ascend C实现)
   - 简化GDN的前向计算流程

3. **变长序列支持增强**
   - 新增 `varlen_to_nonvarlen` 和 `nonvarlen_to_varlen` 辅助函数
   - 支持变长序列在不同格式间的转换

---

### 2.2 Ascend C GDN融合算子

**新增文件**:

| 文件 | 位置 | 行数 | 功能 |
|------|------|------|------|
| `flash_gated_delta_rule.py` | `chunk_gated_delta_rule_npu/` | 529行 | Ascend C实现的GDN融合算子（支持fwd/bwd） |
| `chunk_delta_h.py` | `triton/` | 552行 | Delta头计算Triton实现 |
| `chunk_o.py` | `triton/` | 563行 | 输出块计算Triton实现 |

**核心功能**:
- 前向传播: `flash_gated_delta_rule_fwd`
- 反向传播: `flash_gated_delta_rule_bwd`
- 变长序列支持: `prepare_chunk_indices`, `prepare_wy_repr_bwd`, `recompute_w_u_fwd`

---

### 2.3 Causal Conv1d 优化

**修改文件**: `xtuner/v1/module/attention/causal_conv1d.py`

**优化内容**:

1. **消除冗余transpose**
   - 在GatedDeltaNet中直接使用 `causal_conv1d_triton`
   - 避免causal_conv1d前后的transpose操作

2. **Triton版本实现**
   - 新增 `causal_conv1d_triton` 函数，支持变长序列（cu_seqlens参数）
   - 提升因果卷积的计算效率

3. **混合精度支持**
   - 优化dtype处理，减少不必要的cast操作

---

### 2.4 Cross Entropy Loss Triton实现

**新增文件**: `xtuner/v1/loss/loss_fn_triton.py` (511行)

**核心功能**:

1. **大词汇表分块处理**
   - `fused_ce_loss_kernel_chunked`: 解决UB溢出问题
   - 支持vocab_size > 8K的大词汇表场景
   - 分块计算softmax（每块VOCAB_CHUNK_SIZE）

2. **自动微分支持**
   - `FusedCrossEntropyLoss`: 自定义PyTorch Function
   - 实现前向和反向传播
   - `chunk_loss_bw_kernel`: 反向传播Triton kernel

3. **数值稳定性**
   - 解决标签范围错误
   - 正确处理ignore_index
   - 使用safe softmax实现

**新增文件**: `xtuner/v1/loss/ce_loss.py` (36行)
- 扩展Cross Entropy Loss功能接口

---

### 2.5 CPU/NPU绑核优化

**新增文件**:

| 文件 | 行数 | 功能 |
|------|------|------|
| `cpu_binder.py` | 577行 | CPU/NPU绑核核心模块 |
| `cpu_binder.json` | 26行 | 绑核配置文件 |
| `hyf_test/bind_irq.sh` | 94行 | NPU中断绑核脚本 |

**核心功能**:

1. **多层次绑核**
   - 进程级别: main进程及子进程
   - 线程级别: acl_thread、release_thread
   - 中断级别: SQE/CQ中断绑定到指定CPU

2. **设备感知**
   - 支持Docker环境下Ngid感知
   - 自动获取主进程PID列表

3. **NPU中断优化**
   - `bind_irq.sh`: 自动绑定NPU卡的中断到指定CPU核心
   - 停止irqbalance服务避免冲突
   - 支持多卡环境（0-15卡）

**集成**: `xtuner/v1/train/trainer.py`
- 在训练第一步（cur_step==1）自动执行绑核

---

### 2.6 配置和测试脚本优化

**修改文件**: `hyf_test/config_gpu.py`
- 优化器配置: `AdamWConfig(lr=6e-05, foreach=False, swap_optimizer=False)`
- Loss配置: `CELossConfig(mode="chunk", chunk_size=2048)`
- Global batch size: 32
- Profile step: 20
- 新增确定性训练seed配置

**修改文件**: `hyf_test/test_simple_qwen35_35b.sh`
- 新增CANN 8.5.0-2环境变量配置
- 新增torch_npu和sgl_kernel_npu wheel安装
- 分布式配置: NNODES=2, NPROC_PER_NODE=16
- HCCL BUFFSIZE: 256
- 新增TRITON_ALL_BLOCKS_PARALLEL配置
- 调整MASTER_PORT为23452

**其他配置**:
- `.gitignore`: 新增kernel_meta目录
- `hyf_test/bind_irq.sh`: 新增NPU中断绑核脚本
- `kernel_meta/buildPidInfo.json`: 新增构建信息

---

## 三、代码统计

| 指标 | 数值 |
|------|------|
| 新增文件 | ~15个 |
| 修改文件 | ~7个 |
| 新增代码行数 | ~10,600行 |
| 删除代码行数 | ~99行 |

**核心新增目录**:
- `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/flash_gated_delta_rule.py`
- `xtuner/v1/loss/loss_fn_triton.py`
- `cpu_binder.py`

---

## 四、关键技术亮点

1. **Ascend C融合算子**: 基于Ascend C实现的GDN融合算子，提升NPU计算效率

2. **大词汇表支持**: Cross Entropy Loss Triton实现支持超大词汇表（>8K）的分块计算

3. **细粒度绑核**: 支持进程、线程、中断多级别的CPU/NPU绑核优化，训练速度从25s/step提升到21.7s/step

4. **因果卷积优化**: 消除冗余transpose操作，提升causal_conv1d计算效率

5. **数值稳定性**: GDN使用npu_rms_norm替代手动实现，提高计算精度和稳定性

---

## 五、Commit记录

| Commit | 描述 |
|--------|------|
| `e3a8ff99` | 消除causal_conv1d前后的transpose和GDNRMSNormGated的cast，chunkloss的triton版本可用 |
| `3d44bdad` | ascendc的gdn精度正常，添加了chunkLoss的triton |
| `d0b95a03` | fastest 21.7s |
| `2773acfd` | bindcore faster 25s to 22s |
| `67beaa2c` | using ascend c |
| `da0c8820` | 使用mojo的triton算子，64k，sp2，swap，mtp |
| `73e4a976` | 修改causal_conv1d和固定vision确定性 |

---

## 六、文件变更清单

### 6.1 新增文件

| 文件路径 | 说明 |
|----------|------|
| `cpu_binder.py` | CPU/NPU绑核核心模块 |
| `cpu_binder.json` | 绑核配置文件 |
| `kernel_meta/buildPidInfo.json` | 构建信息 |
| `hyf_test/bind_irq.sh` | NPU中断绑核脚本 |
| `xtuner/v1/loss/ce_loss.py` | Cross Entropy Loss扩展 |
| `xtuner/v1/loss/loss_fn_triton.py` | Cross Entropy Loss Triton实现 |
| `xtuner/v1/module/attention/causal_conv1d.py` | Causal Conv1d Triton实现 |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/flash_gated_delta_rule.py` | Ascend C GDN融合算子 |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/chunk_delta_h.py` | Delta头计算Triton |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/chunk_o.py` | 输出计算Triton |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/chunk_scaled_dot_kkt.py` | SDP KKT计算Triton |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/cumsum.py` | 前缀和Triton |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/solve_tril.py` | 下三角求解Triton |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/utils.py` | Triton工具函数 |
| `xtuner/v1/module/attention/chunk_gated_delta_rule_npu/triton/wy_fast.py` | Wyoming快速算法Triton |

### 6.2 修改文件

| 文件路径 | 主要变更 |
|----------|----------|
| `.gitignore` | 新增kernel_meta目录 |
| `hyf_test/config_gpu.py` | 优化器和Loss配置调整 |
| `hyf_test/test_simple_qwen35_35b.sh` | CANN环境变量和分布式配置 |
| `xtuner/v1/module/attention/gated_deltanet.py` | GDN算子优化，切换到Ascend C实现 |
| `xtuner/v1/ops/rms_norm/__init__.py` | RMS Norm接口调整 |
| `xtuner/v1/train/trainer.py` | 集成CPU绑核功能 |

---

*报告生成时间: 2026-04-09*
